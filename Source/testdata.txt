OF {
	ma:z=x1+x2+6+1/3
}
ST {
	x1 + 9/14 x2 <= 51/14;
	-2 x1 + x2>= -1/3;
	x1>=0;
	x2>=0
}



// 以下是注释区（其实这个双斜杠//符号打不打都行，这后面的部分程序读进去只会认为是没用的部分）

// 目标函数OF只能有一条，如果分号分隔多条的话会报错

// 变量名规定最多只能是三个字符! 第一位是字母，后面两位是数字，如：x1,x2,x12,x99，多余的会被忽略
// 系数可以是整数、分数、小数，
// 写分数的时候不要把符号写在分母上，另外写成分数的话分子分母都得是整数
// 大括号中每一项请务必用分号;分隔，不然可能有意想不到的结果

// 决策变量有取值正负情况的时候只支持 x >= 0; x <= 0 这种写法，其他的一律会被当做约束条件
// 无约束(unr)变量无需写出，程序自动标记
// 单纯形法要求所有决策变量非负呢，x<=0的变量在标准化时会被临时替换为-x>=0，最终结果中再换回来
// 选择整数规划的时候，所有决策变量都会自动新增整数约束
// (如果将来会写0-1整数规划的话）所有决策变量自动约束为只能取0,1