CONSTANTS {
	M;C;
}

LF {
	max:z  =   2x1+3Cxydxc2+8+2Mx3+6x4+5M
}
ST {
	x1 + 2x2 <= 8;
	4x1      <= 16;
	       4x1<=12;
	x1>=0;
	x2>=0
}

// CONSTANTS中的常量只支持一个英文字母a-zA-Z进行表示
// 变量名不要数字开头！规定最多只能是两个字符：x1,x2，多余的会被忽略
// 为了处理方便，价值系数在这里规定全部为整数