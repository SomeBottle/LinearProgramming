CONSTANTS {
	M;C
}

LF {
	max:z  =   -2.45 x1 + 1.35 x2
}
ST {
	x1 + 2x2 <= -1.35;
	4x1      <= 16;
	       4x2<=12;
	x1>=0;
	x2>=0
}

// CONSTANTS中的常量只支持一个英文字母a-zA-Z进行表示
// 变量名不要数字开头！规定最多只能是两个字符：x1,x2，多余的会被忽略
// 价值系数可以是整数、分数、小数，
// 写分数的时候不要把符号写在分母上，另外写成分数分子分母都得是整数
// 大括号中每一项请务必用分号;分隔，不然可能有意想不到的结果